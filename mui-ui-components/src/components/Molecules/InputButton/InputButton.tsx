import React from "react";
import { Button, Input } from "../../Atoms";

const InputButton = () => {
  return (
    <div>
      <Input label="인풋"/>
      <Button label="버튼"/>
    </div>
  );
};

export default InputButton;
